@model IEnumerable<k22cnt3_tdt_2210900134.Models.thong_bao>

@{
    ViewBag.Title = "Thông báo";
}

<h2>Thông báo</h2>

<p>
    @Html.ActionLink("Thêm", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.noi_dung)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nguoi_dung.ten_dang_nhap)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.noi_dung)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nguoi_dung.ten_dang_nhap)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ma_thong_bao }, new { @class = "btn btn-success" }) |
            @Html.ActionLink("Details", "Details", new { id=item.ma_thong_bao }, new { @class = "btn btn-primary" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ma_thong_bao }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
