@model IEnumerable<k22cnt3_tdt_2210900134.Models.dich_vu>

@{
    ViewBag.Title = "Danh Sách Dịch Vụ";
}

<h2>Danh Sách Dịch Vụ</h2>

<p>
    @Html.ActionLink("Thêm Mới", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().ten_dich_vu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().mo_ta)
            </th>
            <th>Hành Động</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ten_dich_vu)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.mo_ta)
                </td>
                <td>
                    @Html.ActionLink("Chỉnh Sửa", "Edit", new { id = item.ma_dich_vu }, new { @class = "btn btn-success btn-sm" }) |
                    @Html.ActionLink("Chi Tiết", "Details", new { id = item.ma_dich_vu }, new { @class = "btn btn-info btn-sm" }) |
                    @Html.ActionLink("Xóa", "Delete", new { id = item.ma_dich_vu }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }
    </tbody>
</table>
